import { DisableFormDirective } from './disable-form.directive';

describe('DisableFormDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableFormDirective();
    expect(directive).toBeTruthy();
  });
});
